[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "azazel"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "click >= 8.1",
]

[project.scripts]
azazel = "azazel.interface:main"

[project.optional-dependencies]
dev = [
  "coverage",
]
