[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "azazel"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
azazel = "azazel.server:main"

[project.optional-dependencies]
dev = [
  "coverage",
]
